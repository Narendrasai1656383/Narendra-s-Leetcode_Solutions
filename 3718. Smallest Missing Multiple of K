class Solution {
    public int missingMultiple(int[] nums, int k) {
        Arrays.sort(nums);
        int cur = k;
        for(int i=0; i<nums.length;i++){
            if(nums[i] == cur){
                 cur += k;
            }
        }
        return cur;
    }
}
